version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu/bluetags
    overwrite: yes

permissions:
  - object: /home/ubuntu/bluetags
    pattern: "**"
    owner: ubuntu
    group: ubuntu
